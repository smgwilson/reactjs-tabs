{
  "name": "tabs",
  "description": "A ReactJs project to implement Navigational Tabs that render child content.",
  "version": "0.0.84",
  "dependencies": {
    "react-dom": "16.0.0",
    "react": "16.0.0"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}